from pathlib import Path

# 默认输入输出路径
默认评分文件路径 = Path("../电影推荐项目_第一步/output/数据/评分表_预处理后.csv")
默认输出目录 = Path("output")

# 数据划分参数
默认随机种子 = 42
默认训练集比例 = 0.8
默认验证集比例 = 0.1
默认测试集比例 = 0.1

# ItemCF 默认参数
默认ItemCF邻居数列表 = [5, 10, 20, 40, 80]
默认ItemCF相似度列表 = ["cosine", "pearson"]
默认ItemCF最少共同评分数 = 2

# BiasMF 默认参数
默认MF隐向量维度列表 = [8, 16, 32]
默认MF学习率列表 = [0.001, 0.005]
默认MF正则化列表 = [0.01, 0.05]
默认MF训练轮数 = 20

# SVD++ 默认参数
默认SVDPP隐向量维度列表 = [8, 16, 32]
默认SVDPP学习率列表 = [0.001, 0.005]
默认SVDPP正则化列表 = [0.01, 0.05]
默认SVDPP训练轮数 = 20

# GNN 默认参数
# 用户和电影特征路径默认留空，运行时根据 ratings-path 自动推断
# 默认GNN模型列表 = ["gcn", "graphsage"]
# 默认GNN隐藏维度列表 = [32, 64]
# 默认GNN层数列表 = [1, 2]
# 默认GNN学习率列表 = [0.001, 0.005]
# 默认GNN权重衰减列表 = [0.0001, 0.00001]
# 默认GNN训练轮数 = 40
# 默认GNNDropout = 0.1
# 默认GNN设备 = ""

# 当前默认值已设为已验证的最优 GCN 参数
默认GNN模型列表 = ["gcn"]
默认GNN隐藏维度列表 = [64]
默认GNN层数列表 = [1]
默认GNN学习率列表 = [0.003]
默认GNN权重衰减列表 = [0.00005]
默认GNN训练轮数 = 40
默认GNNDropout = 0.1
默认GNN设备 = ""
